# Sign Language Recognition — FastAPI Backend Dependencies (Runtime)
# Install with: pip install -r requirements.txt

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# ML / Computer Vision
# tflite-runtime is ~2MB vs ~500MB for full tensorflow — fits in Render free tier
ai-edge-litert>=1.2.0
mediapipe>=0.10.9
opencv-python-headless>=4.9.0.80
numpy>=1.26.0,<2.0.0

# Data / Model
pydantic>=2.0.0
scikit-learn>=1.4.0
